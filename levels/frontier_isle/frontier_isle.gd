## Driftspar — the first frontier isle, reachable only by sailing a voyage
## from the Skydock. A sparse, untouched rock for now (scenery + a way
## home); it's the proof that owning a ship opens the map. Future content
## (an NPC, a puzzle host, a reward) drops in here. Shared movement / spawn
## behavior comes from [BaseLocation]. See [[voyage-loop-research]].
class_name FrontierIsle
extends BaseLocation